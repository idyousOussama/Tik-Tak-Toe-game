<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="176dp"
    android:gravity="top"
    android:padding="10dp"
    android:background="@color/deep_blue"
    android:theme="@style/ThemeOverlay.AppCompat.Dark"
    android:orientation="vertical"
    >

    <de.hdodenhof.circleimageview.CircleImageView

        android:id="@+id/nav_user_Image"
        android:layout_width="80dp"
        android:layout_height="80dp"
        android:src="@color/gray"

        android:layout_marginTop="50dp"
        android:layout_marginStart="10dp"
        />
    <TextView
        android:id="@+id/nav_user_name_text"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Oussama_idyouse"
    android:textColor="@color/white"
        android:textSize="20sp"
        android:gravity="center"
        android:layout_toEndOf="@+id/nav_user_Image"
        android:layout_alignTop="@+id/nav_user_Image"
        android:layout_alignBottom="@+id/nav_user_Image"
        android:layout_marginStart="10dp"
        android:fontFamily="@font/poppins"/>

</RelativeLayout>